---
phase: 05-scsi-command-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/scsi/opcode.go
  - internal/scsi/sense.go
  - internal/scsi/sense_test.go
  - internal/scsi/commands.go
  - internal/scsi/commands_test.go
  - internal/scsi/inquiry.go
  - internal/scsi/inquiry_test.go
  - internal/scsi/capacity.go
  - internal/scsi/capacity_test.go
  - internal/scsi/modesense.go
  - internal/scsi/modesense_test.go
autonomous: true
requirements:
  - SCSI-01
  - SCSI-02
  - SCSI-04
  - SCSI-07
  - SCSI-08
  - SCSI-09
  - SCSI-10

must_haves:
  truths:
    - "SenseKey enum has all 14 values with correct numeric codes and String() method"
    - "ParseSense handles both fixed (0x70/0x71) and descriptor (0x72/0x73) formats"
    - "CommandError wraps status + sense with IsSenseKey helper"
    - "TEST UNIT READY produces 6-byte CDB with opcode 0x00 and all zeros"
    - "INQUIRY standard produces CDB with opcode 0x12, EVPD=0, correct allocation length"
    - "ParseInquiry returns typed struct with DeviceType, Vendor, Product, Revision"
    - "READ CAPACITY(10) produces CDB opcode 0x25, parse returns LastLBA + BlockSize"
    - "READ CAPACITY(16) produces CDB opcode 0x9E with service action 0x10"
    - "REQUEST SENSE produces CDB opcode 0x03 with allocation length"
    - "REPORT LUNS produces CDB opcode 0xA0 with allocation length in bytes 6-9"
    - "ParseReportLuns returns []uint64 LUN list"
    - "MODE SENSE(6) and (10) produce correct CDBs with page code and DBD flag"
  artifacts:
    - path: "internal/scsi/opcode.go"
      provides: "Opcode constants, SenseKey enum, Option type, SCSI status constants, CommandError"
      contains: "OpTestUnitReady"
    - path: "internal/scsi/sense.go"
      provides: "ParseSense, SenseData struct, ASC/ASCQ lookup"
      exports: ["ParseSense", "SenseData", "SenseKey", "IsSenseKey"]
    - path: "internal/scsi/inquiry.go"
      provides: "Inquiry CDB builder and response parser"
      exports: ["Inquiry", "ParseInquiry"]
    - path: "internal/scsi/capacity.go"
      provides: "ReadCapacity10/16 CDB builders and parsers"
      exports: ["ReadCapacity10", "ReadCapacity16", "ParseReadCapacity10", "ParseReadCapacity16"]
    - path: "internal/scsi/commands.go"
      provides: "TestUnitReady, RequestSense, ReportLuns, ParseReportLuns"
      exports: ["TestUnitReady", "RequestSense", "ReportLuns", "ParseReportLuns"]
    - path: "internal/scsi/modesense.go"
      provides: "ModeSense6/10 CDB builders and parsers"
      exports: ["ModeSense6", "ModeSense10", "ParseModeSense6", "ParseModeSense10"]
  key_links:
    - from: "internal/scsi/opcode.go"
      to: "internal/session/types.go"
      via: "session.Command and session.Result types"
      pattern: "session\\.Command"
    - from: "internal/scsi/sense.go"
      to: "internal/scsi/opcode.go"
      via: "SenseKey type and CommandError"
      pattern: "SenseKey|CommandError"
---

<objective>
Create the internal/scsi/ package foundation: shared types (opcodes, status codes, sense keys, functional options, CommandError), sense data parsing (fixed + descriptor formats with ASC/ASCQ lookup), and core SCSI command CDB builders + response parsers for TEST UNIT READY, INQUIRY (standard), READ CAPACITY 10/16, REQUEST SENSE, REPORT LUNS, and MODE SENSE 6/10.

Purpose: Establish the package structure and foundational types that all subsequent SCSI commands depend on, plus the most commonly used device discovery commands.
Output: 7 source files + 5 test files in internal/scsi/
</objective>

<execution_context>
@/home/rkujawa/repos/uiscsi/.claude/get-shit-done/workflows/execute-plan.md
@/home/rkujawa/repos/uiscsi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From internal/session/types.go:
```go
type Command struct {
    CDB                    [16]byte
    Read                   bool
    Write                  bool
    ExpectedDataTransferLen uint32
    LUN                    uint64
    TaskAttributes         uint8
    Data io.Reader
}

type Result struct {
    Status        uint8
    SenseData     []byte
    Data          io.Reader
    Overflow      bool
    Underflow     bool
    ResidualCount uint32
    Err           error
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Foundation types, sense parsing, and CommandError</name>
  <files>internal/scsi/opcode.go, internal/scsi/sense.go, internal/scsi/sense_test.go</files>
  <read_first>internal/session/types.go, .planning/phases/05-scsi-command-layer/05-RESEARCH.md</read_first>
  <behavior>
    - ParseSense with 0x70 response code, 18+ bytes: returns SenseKey, ASC, ASCQ, Valid, Information correctly
    - ParseSense with 0x72 response code, 8+ bytes: returns SenseKey at byte 1 bits 3-0, ASC at byte 2, ASCQ at byte 3
    - ParseSense with 0x71 (deferred fixed): parses same as 0x70
    - ParseSense with 0x73 (deferred descriptor): parses same as 0x72
    - ParseSense with too-short data (<2 bytes): returns error
    - ParseSense with unknown response code (0x7E): returns error
    - SenseData.String() returns "SENSE_KEY: asc/ascq description" for known codes
    - SenseData.String() returns "SENSE_KEY: UNKNOWN (0xNN/0xNN)" for unknown ASC/ASCQ
    - IsSenseKey returns true when CommandError has matching key
    - IsSenseKey returns false for non-CommandError errors
    - SenseKey.String() returns human names for all 14 keys
    - CommandError.Error() includes status hex and sense string
  </behavior>
  <action>
    Create `internal/scsi/` directory.

    **opcode.go** - Per D-01, new `internal/scsi/` package. Contains:

    1. Opcode constants (per research CDB reference):
       - OpTestUnitReady = 0x00, OpRequestSense = 0x03, OpInquiry = 0x12, OpModeSense6 = 0x1A,
         OpStartStopUnit = 0x1B, OpReadCapacity10 = 0x25, OpRead10 = 0x28, OpWrite10 = 0x2A,
         OpVerify10 = 0x2F, OpSynchronizeCache10 = 0x35, OpWriteSame10 = 0x41, OpUnmap = 0x42,
         OpModeSense10 = 0x5A, OpPersistReserveIn = 0x5E, OpPersistReserveOut = 0x5F,
         OpRead16 = 0x88, OpCompareAndWrite = 0x89, OpWrite16 = 0x8A, OpVerify16 = 0x8F,
         OpSynchronizeCache16 = 0x91, OpWriteSame16 = 0x93, OpServiceActionIn16 = 0x9E,
         OpReportLuns = 0xA0

    2. SCSI status constants (per SAM-5):
       - StatusGood = 0x00, StatusCheckCondition = 0x02, StatusConditionMet = 0x04,
         StatusBusy = 0x08, StatusReservationConflict = 0x18, StatusTaskSetFull = 0x28,
         StatusACAActive = 0x30, StatusTaskAborted = 0x40

    3. SenseKey type (uint8) with all 14 constants:
       - SenseNoSense=0x00, SenseRecoveredError=0x01, SenseNotReady=0x02, SenseMediumError=0x03,
         SenseHardwareError=0x04, SenseIllegalRequest=0x05, SenseUnitAttention=0x06,
         SenseDataProtect=0x07, SenseBlankCheck=0x08, SenseVendorSpecific=0x09,
         SenseCopyAborted=0x0A, SenseAbortedCommand=0x0B, SenseVolumeOverflow=0x0D,
         SenseMiscompare=0x0E
       - String() method with human-readable names

    4. Option type and options struct per D-04:
       ```go
       type Option func(*options)
       type options struct {
           fua, dpo, immed, anchor, unmap, ndob bool
           bytchk uint8
       }
       func WithFUA() Option  { return func(o *options) { o.fua = true } }
       func WithDPO() Option  { return func(o *options) { o.dpo = true } }
       func WithImmed() Option { return func(o *options) { o.immed = true } }
       func WithAnchor() Option { return func(o *options) { o.anchor = true } }
       func WithUnmap() Option { return func(o *options) { o.unmap = true } }
       func WithNDOB() Option { return func(o *options) { o.ndob = true } }
       func WithBytchk(v uint8) Option { return func(o *options) { o.bytchk = v } }
       ```

    5. CommandError per D-06:
       ```go
       type CommandError struct {
           Status uint8
           Sense  *SenseData
       }
       func (e *CommandError) Error() string // "scsi: status 0x02: MEDIUM ERROR: Unrecovered read error"
       func IsSenseKey(err error, key SenseKey) bool // uses errors.As
       ```

    6. Internal helper: `checkResult(result session.Result) ([]byte, error)` that checks result.Err and result.Status, returns data bytes via io.ReadAll, or returns CommandError with parsed sense.

    **sense.go** - Per D-06, sense data parsing:

    1. SenseData struct:
       - ResponseCode uint8, Key SenseKey, ASC uint8, ASCQ uint8, Information uint32, Valid bool, Raw []byte

    2. ParseSense(data []byte) (*SenseData, error):
       - Check len < 2 -> error
       - Response code = data[0] & 0x7F
       - 0x70/0x71 (fixed format): need >= 18 bytes. Valid = data[0]&0x80. Key = data[2]&0x0F. Information = BigEndian.Uint32(data[3:7]). ASC = data[12]. ASCQ = data[13].
       - 0x72/0x73 (descriptor format): need >= 8 bytes. Key = data[1]&0x0F. ASC = data[2]. ASCQ = data[3].
       - Unknown -> error
       - Raw = defensive copy of input

    3. SenseData.String() method using ASC/ASCQ lookup table (~50 common codes from research).

    4. ascLookup map with entries from research: 0x04/0x00 "Not ready, cause not reportable", 0x04/0x01 "Becoming ready", 0x08/0x00 "Communication failure", 0x11/0x00 "Unrecovered read error", 0x20/0x00 "Invalid command operation code", 0x24/0x00 "Invalid field in CDB", 0x26/0x00 "Invalid field in parameter list", 0x27/0x00 "Write protected", 0x28/0x00 "Not ready to ready change", 0x29/0x00 "Power on, reset, or bus device reset occurred", 0x2A/0x00 "Mode parameters changed", 0x2F/0x00 "Commands cleared by another initiator", 0x3A/0x00 "Medium not present", 0x44/0x00 "Internal target failure", plus additional common codes.

    **sense_test.go** - Table-driven tests per established pattern:
    - TestParseSense: fixed format (0x70), deferred fixed (0x71), descriptor (0x72), deferred descriptor (0x73), short data, unknown response code
    - TestSenseDataString: known ASC/ASCQ, unknown ASC/ASCQ
    - TestIsSenseKey: matching, non-matching, non-CommandError
    - TestSenseKeyString: all 14 values
  </action>
  <verify>
    <automated>cd /home/rkujawa/repos/uiscsi && go test ./internal/scsi/ -run "TestParseSense|TestSenseData|TestIsSenseKey|TestSenseKey|TestCommandError" -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "OpTestUnitReady.*= 0x00" internal/scsi/opcode.go
    - grep -q "StatusCheckCondition.*= 0x02" internal/scsi/opcode.go
    - grep -q "SenseIllegalRequest.*= 0x05" internal/scsi/opcode.go
    - grep -q "func ParseSense" internal/scsi/sense.go
    - grep -q "0x70, 0x71" internal/scsi/sense.go
    - grep -q "0x72, 0x73" internal/scsi/sense.go
    - grep -q "func IsSenseKey" internal/scsi/opcode.go
    - grep -q "type CommandError struct" internal/scsi/opcode.go
    - grep -q "type Option func" internal/scsi/opcode.go
    - grep -q "WithFUA" internal/scsi/opcode.go
    - go test ./internal/scsi/ -run "TestParseSense" -count=1 exits 0
  </acceptance_criteria>
  <done>ParseSense handles both fixed and descriptor formats. SenseKey has all 14 values with String(). CommandError wraps status+sense with IsSenseKey helper. ~50 ASC/ASCQ codes in lookup table. All sense tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Core command CDB builders and response parsers</name>
  <files>internal/scsi/commands.go, internal/scsi/commands_test.go, internal/scsi/inquiry.go, internal/scsi/inquiry_test.go, internal/scsi/capacity.go, internal/scsi/capacity_test.go, internal/scsi/modesense.go, internal/scsi/modesense_test.go</files>
  <read_first>internal/scsi/opcode.go, internal/scsi/sense.go, internal/session/types.go, .planning/phases/05-scsi-command-layer/05-RESEARCH.md</read_first>
  <behavior>
    - TestUnitReady(lun): CDB = [0x00, 0x00, 0x00, 0x00, 0x00, 0x00, ...zeros], Read=false, Write=false, LUN set
    - Inquiry(lun, allocLen): CDB = [0x12, 0x00, 0x00, allocLen>>8, allocLen&0xFF, 0x00, ...], Read=true, ExpectedDataTransferLen=allocLen
    - ParseInquiry: 36-byte response -> DeviceType (byte0 & 0x1F), Vendor (bytes 8-15 trimmed), Product (bytes 16-31 trimmed), Revision (bytes 32-35 trimmed), AdditionalLength (byte 4)
    - ParseInquiry: short response (<36 bytes) -> error
    - ParseInquiry: status != GOOD -> CommandError with sense
    - ReadCapacity10(lun): CDB = [0x25, 0x00, ...zeros], Read=true, ExpectedDataTransferLen=8
    - ParseReadCapacity10: 8-byte response -> LastLBA (bytes 0-3), BlockSize (bytes 4-7)
    - ReadCapacity16(lun, allocLen): CDB = [0x9E, 0x10, ...zeros, allocLen in bytes 10-13], Read=true (per D-03 positional args)
    - ParseReadCapacity16: 32-byte response -> LastLBA (bytes 0-7, uint64), BlockSize (bytes 8-11), protection/provisioning info
    - RequestSense(lun, allocLen): CDB = [0x03, 0x00, 0x00, 0x00, allocLen, 0x00]
    - ReportLuns(allocLen): CDB = [0xA0, ...zeros, allocLen in bytes 6-9], Read=true
    - ParseReportLuns: 8-byte header (LUN list length) + 8-byte LUN entries -> []uint64
    - ModeSense6(lun, pageCode, subpageCode, allocLen, opts): CDB = [0x1A, dbd_flag, (pc<<6)|pageCode, subpageCode, allocLen, 0x00]
    - ModeSense10(lun, pageCode, subpageCode, allocLen, opts): CDB = [0x5A, dbd_flag, (pc<<6)|pageCode, subpageCode, ...zeros, allocLen in bytes 7-8]
    - ParseModeSense6: header (4 bytes: mode data length, medium type, device-specific, block descriptor length) + block descriptors + mode pages as raw bytes
    - ParseModeSense10: header (8 bytes) + block descriptors + mode pages as raw bytes
  </behavior>
  <action>
    Per D-03 (plain functions returning session.Command) and D-07 (standalone functions, no Session methods):

    **commands.go** - TEST UNIT READY, REQUEST SENSE, REPORT LUNS:

    1. `TestUnitReady(lun uint64) session.Command`:
       - CDB byte 0 = 0x00 (OpTestUnitReady). Bytes 1-5 = 0. No data transfer.
       - Set cmd.LUN = lun. Read=false, Write=false, ExpectedDataTransferLen=0.

    2. `RequestSense(lun uint64, allocLen uint8) session.Command`:
       - CDB byte 0 = 0x03 (OpRequestSense). Byte 4 = allocLen. Read=true.
       - ExpectedDataTransferLen = uint32(allocLen). LUN = lun.

    3. `ReportLuns(allocLen uint32) session.Command`:
       - CDB byte 0 = 0xA0 (OpReportLuns). Bytes 6-9 = allocLen (BigEndian). Read=true.
       - LUN = 0 (REPORT LUNS targets the target, not a specific LUN).
       - ExpectedDataTransferLen = allocLen.

    4. `ParseReportLuns(result session.Result) ([]uint64, error)`:
       - Use checkResult helper. First 4 bytes = LUN list length (L). Then L bytes of 8-byte LUN entries.
       - Return slice of uint64 LUNs. Error if data shorter than header.

    **inquiry.go** - INQUIRY standard per D-03, D-05, D-08:

    1. `Inquiry(lun uint64, allocLen uint16) session.Command`:
       - CDB byte 0 = 0x12 (OpInquiry). Byte 1 = 0 (EVPD=0). Byte 2 = 0 (page code).
       - Bytes 3-4 = allocLen (BigEndian). Read=true. LUN = lun.

    2. `InquiryResponse` struct per D-05:
       - PeripheralDeviceType uint8, PeripheralQualifier uint8
       - Vendor string (trimmed), Product string (trimmed), Revision string (trimmed)
       - AdditionalLength uint8, Raw []byte

    3. `ParseInquiry(result session.Result) (*InquiryResponse, error)`:
       - Use checkResult. Require >= 36 bytes. Per Pitfall 9: trim trailing spaces with strings.TrimRight.
       - DeviceType = data[0] & 0x1F, Qualifier = (data[0] >> 5) & 0x07
       - Vendor = string(data[8:16]) trimmed, Product = string(data[16:32]) trimmed, Revision = string(data[32:36]) trimmed

    **capacity.go** - READ CAPACITY per D-03:

    1. `ReadCapacity10(lun uint64) session.Command`:
       - CDB byte 0 = 0x25 (OpReadCapacity10). No other fields needed (obsolete LBA field left 0).
       - Read=true, ExpectedDataTransferLen=8. LUN = lun.

    2. `ReadCapacity10Response` struct: LastLBA uint32, BlockSize uint32

    3. `ParseReadCapacity10(result session.Result) (*ReadCapacity10Response, error)`:
       - Require 8 bytes. LastLBA = BigEndian.Uint32(data[0:4]). BlockSize = BigEndian.Uint32(data[4:8]).

    4. `ReadCapacity16(lun uint64, allocLen uint32) session.Command`:
       - Per Pitfall 1: CDB byte 0 = 0x9E (OpServiceActionIn16), byte 1 = 0x10 (service action).
       - Bytes 10-13 = allocLen (BigEndian). Read=true. LUN = lun.
       - ExpectedDataTransferLen = allocLen.

    5. `ReadCapacity16Response` struct: LastLBA uint64, BlockSize uint32, ProtectionEnabled bool, ProtectionType uint8, LogicalBlocksPerPhysicalBlock uint8, LowestAlignedLBA uint16, Raw []byte

    6. `ParseReadCapacity16(result session.Result) (*ReadCapacity16Response, error)`:
       - Require 32 bytes minimum. LastLBA = BigEndian.Uint64(data[0:8]). BlockSize = BigEndian.Uint32(data[8:12]).
       - ProtectionEnabled = data[12]&0x01 != 0. ProtectionType = (data[12] >> 1) & 0x07.
       - LogicalBlocksPerPhysicalBlock = data[13] & 0x0F. LowestAlignedLBA = BigEndian.Uint16(data[14:16]) & 0x3FFF.

    **modesense.go** - MODE SENSE per D-03, D-05:

    1. Add option: `WithDBD() Option` (Disable Block Descriptors, bit 3 of byte 1).
       Add option: `WithPageControl(pc uint8) Option` (bits 7-6 of page code byte).

    2. `ModeSense6(lun uint64, pageCode, subpageCode, allocLen uint8, opts ...Option) session.Command`:
       - CDB byte 0 = 0x1A (OpModeSense6). Byte 1 = dbd<<3. Byte 2 = (pc<<6)|pageCode. Byte 3 = subpageCode. Byte 4 = allocLen.
       - Read=true. LUN = lun. ExpectedDataTransferLen = uint32(allocLen).

    3. `ModeSense10(lun uint64, pageCode, subpageCode uint8, allocLen uint16, opts ...Option) session.Command`:
       - CDB byte 0 = 0x5A (OpModeSense10). Byte 1 = dbd<<3. Byte 2 = (pc<<6)|pageCode. Byte 3 = subpageCode. Bytes 7-8 = allocLen.
       - Read=true. LUN = lun.

    4. `ModeSense6Response` struct: ModeDataLength uint8, MediumType uint8, DeviceSpecific uint8, BlockDescriptorLength uint8, BlockDescriptors []byte, Pages []byte, Raw []byte

    5. `ParseModeSense6(result session.Result) (*ModeSense6Response, error)`:
       - Require >= 4 bytes (header). Parse header. BlockDescriptors = data[4:4+bdl]. Pages = data[4+bdl:].

    6. `ModeSense10Response`: same fields but ModeDataLength uint16, BlockDescriptorLength uint16, 8-byte header. LongLBA bool.

    7. `ParseModeSense10(result session.Result) (*ModeSense10Response, error)`:
       - Require >= 8 bytes (header). Parse header. BD start at 8, pages after.

    **Test files** - Table-driven golden byte CDB tests per established pattern:

    - commands_test.go: TestTestUnitReady (CDB bytes, direction flags), TestRequestSense (allocation length placement), TestReportLuns (CDB bytes, allocation length in bytes 6-9), TestParseReportLuns (normal response, empty list, short data)
    - inquiry_test.go: TestInquiry (CDB bytes), TestParseInquiry (normal 36-byte response with spaces trimmed, short response error, CHECK CONDITION with sense)
    - capacity_test.go: TestReadCapacity10 (CDB bytes), TestParseReadCapacity10 (8-byte response), TestReadCapacity16 (CDB bytes verify byte 0=0x9E byte 1=0x10), TestParseReadCapacity16 (32-byte response with protection bits)
    - modesense_test.go: TestModeSense6 (CDB with/without DBD), TestModeSense10, TestParseModeSense6 (header + block descriptors + pages), TestParseModeSense10
  </action>
  <verify>
    <automated>cd /home/rkujawa/repos/uiscsi && go test ./internal/scsi/ -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "func TestUnitReady" internal/scsi/commands.go
    - grep -q "func RequestSense" internal/scsi/commands.go
    - grep -q "func ReportLuns" internal/scsi/commands.go
    - grep -q "func ParseReportLuns" internal/scsi/commands.go
    - grep -q "func Inquiry" internal/scsi/inquiry.go
    - grep -q "func ParseInquiry" internal/scsi/inquiry.go
    - grep -q "func ReadCapacity10" internal/scsi/capacity.go
    - grep -q "func ReadCapacity16" internal/scsi/capacity.go
    - grep -q "0x9E" internal/scsi/capacity.go (service action in opcode)
    - grep -q "0x10" internal/scsi/capacity.go (RC16 service action)
    - grep -q "func ModeSense6" internal/scsi/modesense.go
    - grep -q "func ModeSense10" internal/scsi/modesense.go
    - grep -q "strings.TrimRight" internal/scsi/inquiry.go (Pitfall 9 space trimming)
    - go test ./internal/scsi/ -count=1 exits 0
    - go vet ./internal/scsi/ exits 0
  </acceptance_criteria>
  <done>All core SCSI commands (TUR, INQUIRY standard, READ CAPACITY 10/16, REQUEST SENSE, REPORT LUNS, MODE SENSE 6/10) have CDB builders returning session.Command and response parsers returning typed structs. All tests pass with golden byte CDB verification. RC16 uses service action pattern (Pitfall 1). INQUIRY trims spaces (Pitfall 9).</done>
</task>

</tasks>

<verification>
cd /home/rkujawa/repos/uiscsi && go test ./internal/scsi/ -count=1 -race
cd /home/rkujawa/repos/uiscsi && go vet ./internal/scsi/
</verification>

<success_criteria>
- internal/scsi/ package exists with opcode.go, sense.go, commands.go, inquiry.go, capacity.go, modesense.go
- All 14 SenseKey values defined with String()
- ParseSense handles fixed (0x70/0x71) and descriptor (0x72/0x73) formats
- ~50 ASC/ASCQ lookup entries
- CommandError with IsSenseKey helper
- 7 CDB builder functions: TestUnitReady, Inquiry, ReadCapacity10, ReadCapacity16, RequestSense, ReportLuns, ModeSense6, ModeSense10
- 6 parse functions: ParseInquiry, ParseReadCapacity10, ParseReadCapacity16, ParseReportLuns, ParseModeSense6, ParseModeSense10
- All tests pass under -race
</success_criteria>

<output>
After completion, create `.planning/phases/05-scsi-command-layer/05-01-SUMMARY.md`
</output>
