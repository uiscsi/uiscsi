---
phase: 05-scsi-command-layer
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - internal/scsi/provisioning.go
  - internal/scsi/provisioning_test.go
  - internal/scsi/reservations.go
  - internal/scsi/reservations_test.go
  - internal/scsi/commands.go
  - internal/scsi/commands_test.go
autonomous: true
requirements:
  - SCSI-11
  - SCSI-12
  - SCSI-13
  - SCSI-14
  - SCSI-15
  - SCSI-16
  - SCSI-17
  - SCSI-19

must_haves:
  truths:
    - "SYNCHRONIZE CACHE(10) produces CDB opcode 0x35 with LBA in bytes 2-5, block count in bytes 7-8"
    - "SYNCHRONIZE CACHE(16) produces CDB opcode 0x91 with LBA in bytes 2-9, block count in bytes 10-13"
    - "WRITE SAME(10) produces CDB opcode 0x41 with UNMAP/ANCHOR/NDOB flags in byte 1"
    - "WRITE SAME(16) produces CDB opcode 0x93 with 64-bit LBA in bytes 2-9"
    - "UNMAP produces CDB opcode 0x42 with parameter list length in bytes 7-8"
    - "UnmapBlockDescriptor list is serialized into correct 8-byte header + 16-byte descriptors"
    - "VERIFY(10) and (16) produce correct CDBs with BYTCHK flag"
    - "PERSISTENT RESERVE IN produces CDB opcode 0x5E with service action in byte 1"
    - "ParsePersistReserveInKeys returns reservation keys from response"
    - "PERSISTENT RESERVE OUT produces CDB opcode 0x5F with 24-byte parameter data"
    - "COMPARE AND WRITE produces CDB opcode 0x89 with ExpectedDataTransferLen = 2 * blocks * blockSize"
    - "START STOP UNIT produces CDB opcode 0x1B with power condition and START/LOEJ bits"
  artifacts:
    - path: "internal/scsi/provisioning.go"
      provides: "SynchronizeCache10/16, WriteSame10/16, Unmap CDB builders"
      exports: ["SynchronizeCache10", "SynchronizeCache16", "WriteSame10", "WriteSame16", "Unmap"]
    - path: "internal/scsi/reservations.go"
      provides: "PersistReserveIn/Out CDB builders and response parsers"
      exports: ["PersistReserveIn", "PersistReserveOut", "ParsePersistReserveInKeys", "ParsePersistReserveInReservation"]
    - path: "internal/scsi/commands.go"
      provides: "Verify10/16, CompareAndWrite, StartStopUnit (added to existing file)"
      exports: ["Verify10", "Verify16", "CompareAndWrite", "StartStopUnit"]
  key_links:
    - from: "internal/scsi/provisioning.go"
      to: "internal/scsi/opcode.go"
      via: "WithUnmap, WithAnchor, WithNDOB, WithImmed options"
      pattern: "WithUnmap|WithImmed"
    - from: "internal/scsi/reservations.go"
      to: "internal/session/types.go"
      via: "session.Command with Data io.Reader for PR OUT parameter data"
      pattern: "session\\.Command"
---

<objective>
Implement all extended SCSI commands: SYNCHRONIZE CACHE 10/16, WRITE SAME 10/16, UNMAP, VERIFY 10/16, PERSISTENT RESERVE IN/OUT, COMPARE AND WRITE, and START STOP UNIT. These are the advanced block storage commands for cache management, thin provisioning, reservations, and atomic operations.

Purpose: Complete the full SCSI command layer with all 19 requirements covered.
Output: provisioning.go, reservations.go, updated commands.go + all tests
</objective>

<execution_context>
@/home/rkujawa/repos/uiscsi/.claude/get-shit-done/workflows/execute-plan.md
@/home/rkujawa/repos/uiscsi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-scsi-command-layer/05-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output -->
From internal/scsi/opcode.go (created in Plan 01):
- All opcode constants: OpSynchronizeCache10=0x35, OpSynchronizeCache16=0x91, OpWriteSame10=0x41, OpWriteSame16=0x93, OpUnmap=0x42, OpVerify10=0x2F, OpVerify16=0x8F, OpPersistReserveIn=0x5E, OpPersistReserveOut=0x5F, OpCompareAndWrite=0x89, OpStartStopUnit=0x1B
- Option type with WithFUA, WithDPO, WithImmed, WithAnchor, WithUnmap, WithNDOB, WithBytchk
- CommandError, checkResult helper
- session.Command, session.Result types

From internal/scsi/commands.go (created in Plan 01):
- TestUnitReady, RequestSense, ReportLuns already present -- new functions append to this file
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cache, provisioning, and UNMAP commands</name>
  <files>internal/scsi/provisioning.go, internal/scsi/provisioning_test.go</files>
  <read_first>internal/scsi/opcode.go, internal/session/types.go, .planning/phases/05-scsi-command-layer/05-RESEARCH.md</read_first>
  <behavior>
    - SynchronizeCache10(lun=0, lba=0, blocks=0): CDB=[0x35, 0x00, 0x00,0x00,0x00,0x00, 0x00, 0x00,0x00, 0x00,...], no data transfer
    - SynchronizeCache10 with WithImmed(): CDB byte 1 bit 1 set (0x02)
    - SynchronizeCache10(lun=0, lba=1000, blocks=64): LBA in bytes 2-5, blocks in bytes 7-8
    - SynchronizeCache16(lun=0, lba=0x100000000, blocks=1024): CDB=[0x91,...], LBA in bytes 2-9, blocks in bytes 10-13
    - SynchronizeCache16 with WithImmed(): CDB byte 1 = 0x02
    - WriteSame10(lun=0, lba=0, blocks=100, data, WithUnmap()): CDB=[0x41, 0x08, ...], byte 1 bit 3 = UNMAP, Write=true
    - WriteSame10 with WithAnchor(): CDB byte 1 bit 2 set (0x04)
    - WriteSame10 with WithNDOB(): CDB byte 1 bit 0 set (0x01), no data transfer (Data=nil, Write=false)
    - WriteSame16(lun=0, lba=0x200000000, blocks=0xFFFFFFFF, data): 64-bit LBA, 32-bit block count
    - Unmap(lun, descriptors): CDB=[0x42,...], bytes 7-8 = parameter list length, Write=true, Data = serialized descriptors
    - Unmap parameter data: 8-byte header (2-byte data length, 2-byte BD data length, 4 reserved) + 16-byte descriptors (8-byte LBA, 4-byte count, 4 reserved)
    - Unmap with empty descriptors: still valid, header only
  </behavior>
  <action>
    Per D-03 (plain functions), D-04 (functional options for IMMED/UNMAP/ANCHOR/NDOB):

    **provisioning.go**:

    1. `SynchronizeCache10(lun uint64, lba uint32, blocks uint16, opts ...Option) session.Command`:
       - CDB byte 0 = 0x35 (OpSynchronizeCache10)
       - Byte 1: IMMED at bit 1 (0x02)
       - Bytes 2-5: LBA via BigEndian.PutUint32
       - Byte 6: group number = 0
       - Bytes 7-8: number of blocks via BigEndian.PutUint16
       - No data transfer. Read=false, Write=false, ExpectedDataTransferLen=0. LUN=lun.

    2. `SynchronizeCache16(lun uint64, lba uint64, blocks uint32, opts ...Option) session.Command`:
       - CDB byte 0 = 0x91 (OpSynchronizeCache16)
       - Byte 1: IMMED at bit 1 (0x02)
       - Bytes 2-9: LBA via BigEndian.PutUint64
       - Bytes 10-13: number of blocks via BigEndian.PutUint32
       - No data transfer. LUN=lun.

    3. `WriteSame10(lun uint64, lba uint32, blocks uint16, blockSize uint32, data io.Reader, opts ...Option) session.Command`:
       - CDB byte 0 = 0x41 (OpWriteSame10)
       - Byte 1: UNMAP at bit 3 (0x08), ANCHOR at bit 2 (0x04), NDOB at bit 0 (0x01)
       - Bytes 2-5: LBA. Bytes 7-8: number of blocks.
       - If NDOB set: no data transfer (Write=false, Data=nil, ExpectedDataTransferLen=0)
       - Else: Write=true, Data=data, ExpectedDataTransferLen=blockSize (one block of write data)
       - LUN=lun.

    4. `WriteSame16(lun uint64, lba uint64, blocks uint32, blockSize uint32, data io.Reader, opts ...Option) session.Command`:
       - CDB byte 0 = 0x93 (OpWriteSame16)
       - Byte 1: same flags as WriteSame10
       - Bytes 2-9: LBA. Bytes 10-13: number of blocks.
       - Same NDOB logic for data transfer. LUN=lun.

    5. `UnmapBlockDescriptor` struct:
       ```go
       type UnmapBlockDescriptor struct {
           LBA        uint64
           BlockCount uint32
       }
       ```

    6. `Unmap(lun uint64, descriptors []UnmapBlockDescriptor, opts ...Option) session.Command`:
       - Per Pitfall 6: Build parameter data buffer.
       - Each descriptor: 16 bytes (8-byte LBA + 4-byte count + 4 reserved).
       - Block descriptor data length = len(descriptors) * 16.
       - Header (8 bytes): bytes 0-1 = total data length - 2 (BigEndian.PutUint16), bytes 2-3 = block descriptor data length (BigEndian.PutUint16), bytes 4-7 = 0.
       - Total parameter list length = 8 + len(descriptors)*16.
       - CDB byte 0 = 0x42 (OpUnmap). Byte 1: ANCHOR at bit 0 (0x01).
       - Bytes 7-8: parameter list length via BigEndian.PutUint16.
       - Write=true, Data=bytes.NewReader(paramData), ExpectedDataTransferLen = uint32(paramListLen). LUN=lun.

    **provisioning_test.go** - Table-driven golden byte tests:
    - TestSynchronizeCache10: zero LBA/blocks, with IMMED, specific LBA/blocks
    - TestSynchronizeCache16: 64-bit LBA, IMMED flag
    - TestWriteSame10: with UNMAP flag, ANCHOR flag, NDOB (no data), normal with data
    - TestWriteSame16: 64-bit LBA, all flags
    - TestUnmap: single descriptor, multiple descriptors (verify serialized parameter data), empty descriptors, ANCHOR flag
    - For Unmap tests: read cmd.Data into bytes and verify the serialized parameter data layout
  </action>
  <verify>
    <automated>cd /home/rkujawa/repos/uiscsi && go test ./internal/scsi/ -run "TestSyncCache|TestWriteSame|TestUnmap" -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "func SynchronizeCache10" internal/scsi/provisioning.go
    - grep -q "func SynchronizeCache16" internal/scsi/provisioning.go
    - grep -q "func WriteSame10" internal/scsi/provisioning.go
    - grep -q "func WriteSame16" internal/scsi/provisioning.go
    - grep -q "func Unmap" internal/scsi/provisioning.go
    - grep -q "UnmapBlockDescriptor" internal/scsi/provisioning.go
    - grep -q "0x35" internal/scsi/provisioning.go (sync cache 10)
    - grep -q "0x91" internal/scsi/provisioning.go (sync cache 16)
    - grep -q "0x41" internal/scsi/provisioning.go (write same 10)
    - grep -q "0x42" internal/scsi/provisioning.go (unmap)
    - go test ./internal/scsi/ -run "TestSyncCache" -count=1 exits 0
    - go test ./internal/scsi/ -run "TestUnmap" -count=1 exits 0
  </acceptance_criteria>
  <done>SYNCHRONIZE CACHE 10/16, WRITE SAME 10/16, and UNMAP produce correct CDBs. UNMAP serializes parameter data with 8-byte header + 16-byte descriptors per Pitfall 6. NDOB flag correctly disables data transfer. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reservations, VERIFY, COMPARE AND WRITE, START STOP UNIT</name>
  <files>internal/scsi/reservations.go, internal/scsi/reservations_test.go, internal/scsi/commands.go, internal/scsi/commands_test.go</files>
  <read_first>internal/scsi/opcode.go, internal/scsi/commands.go, internal/session/types.go, .planning/phases/05-scsi-command-layer/05-RESEARCH.md</read_first>
  <behavior>
    - PersistReserveIn(lun, serviceAction=0x00 READ_KEYS, allocLen=256): CDB=[0x5E, 0x00, ...zeros, 0x01,0x00, 0x00], Read=true
    - PersistReserveIn(lun, serviceAction=0x01 READ_RESERVATION, allocLen=256): CDB byte 1 = 0x01
    - ParsePersistReserveInKeys: 8-byte header (generation + additional length) + 8-byte keys -> PRGeneration uint32 + []uint64 keys
    - ParsePersistReserveInReservation: header + 16-byte reservation descriptor -> key, scope, type
    - PersistReserveOut(lun, serviceAction=0x00 REGISTER, scopeType, key, saKey): CDB=[0x5F, 0x00, scopeType in byte 2, ...zeros, paramLen=24 in bytes 5-8], Write=true, 24-byte parameter data
    - PR OUT parameter data: bytes 0-7 = reservation key, bytes 8-15 = service action reservation key, bytes 16-23 = zeros (scope-specific addr, aptpl, etc.)
    - Verify10(lun, lba, blocks): CDB=[0x2F, 0x00, lba 4 bytes, 0x00, blocks 2 bytes, 0x00,...], no data by default
    - Verify10 with WithBytchk(1): CDB byte 1 bits 2-1 = 0x02, requires data
    - Verify16(lun, lba, blocks): CDB=[0x8F,...], 64-bit LBA, 32-bit blocks
    - CompareAndWrite(lun, lba, blocks, blockSize, data): CDB=[0x89,...], byte 13 = blocks, ExpectedDataTransferLen = 2*blocks*blockSize (Pitfall 8), Write=true
    - StartStopUnit(lun, WithImmed()): CDB=[0x1B, 0x01, ...], byte 1 bit 0 = IMMED
    - StartStopUnit with power condition + START bit: CDB byte 4 bits 7-4 = power condition, bit 0 = START, bit 1 = LOEJ
  </behavior>
  <action>
    **reservations.go** - PERSISTENT RESERVE IN/OUT per D-03:

    1. Service action constants:
       ```go
       const (
           PRInReadKeys         = 0x00
           PRInReadReservation  = 0x01
           PRInReportCapabilities = 0x02
           PRInReadFullStatus   = 0x03
           PROutRegister        = 0x00
           PROutReserve         = 0x01
           PROutRelease         = 0x02
           PROutClear           = 0x03
           PROutPreempt         = 0x04
           PROutPreemptAndAbort = 0x05
           PROutRegisterAndIgnore = 0x06
           PROutRegisterAndMove = 0x07
       )
       ```

    2. `PersistReserveIn(lun uint64, serviceAction uint8, allocLen uint16) session.Command`:
       - CDB byte 0 = 0x5E (OpPersistReserveIn). Byte 1 = serviceAction & 0x1F.
       - Bytes 7-8 = allocLen (BigEndian). Read=true. LUN=lun.
       - ExpectedDataTransferLen = uint32(allocLen).

    3. `PRKeysResponse` struct: Generation uint32, Keys []uint64

    4. `ParsePersistReserveInKeys(result session.Result) (*PRKeysResponse, error)`:
       - checkResult. Require >= 8 bytes (header). Generation = BigEndian.Uint32(data[0:4]).
       - Additional length = BigEndian.Uint32(data[4:8]). Keys start at byte 8, each 8 bytes.

    5. `PRReservation` struct: Key uint64, ScopeType uint8 (scope in bits 7-4, type in bits 3-0)

    6. `ParsePersistReserveInReservation(result session.Result) (*PRReservation, error)`:
       - checkResult. Require >= 8 (header) + 16 (reservation descriptor) = 24 bytes.
       - Key at bytes 8-15. ScopeType at byte 21 (scope bits 7-4, type bits 3-0).
       - Return nil reservation (no error) if additional length = 0 (no reservation held).

    7. `PersistReserveOut(lun uint64, serviceAction uint8, scopeType uint8, key, saKey uint64) session.Command`:
       - Per Pitfall 7: CDB byte 0 = 0x5F (OpPersistReserveOut). Byte 1 = serviceAction & 0x1F.
       - Byte 2 = scopeType (scope in bits 7-4, type in bits 3-0).
       - Bytes 5-8 = parameter list length = 24 (BigEndian.PutUint32).
       - 24-byte parameter data: bytes 0-7 = key (BigEndian.PutUint64), bytes 8-15 = saKey (BigEndian.PutUint64), bytes 16-23 = 0.
       - Write=true. Data=bytes.NewReader(paramData). ExpectedDataTransferLen=24. LUN=lun.

    **commands.go** - Add to existing file (VERIFY, COMPARE AND WRITE, START STOP UNIT):

    8. `Verify10(lun uint64, lba uint32, blocks uint16, opts ...Option) session.Command`:
       - CDB byte 0 = 0x2F (OpVerify10). Byte 1: BYTCHK at bits 2-1 (cfg.bytchk << 1).
       - Bytes 2-5: LBA. Bytes 7-8: verification length.
       - If bytchk > 0: needs data (but data not provided by this builder -- caller sets cmd.Data and cmd.Write).
       - Default: no data transfer. LUN=lun.

    9. `Verify16(lun uint64, lba uint64, blocks uint32, opts ...Option) session.Command`:
       - CDB byte 0 = 0x8F (OpVerify16). Same BYTCHK handling.
       - Bytes 2-9: LBA. Bytes 10-13: verification length. LUN=lun.

    10. `CompareAndWrite(lun uint64, lba uint64, blocks uint8, blockSize uint32, data io.Reader) session.Command`:
        - Per Pitfall 8: CDB byte 0 = 0x89 (OpCompareAndWrite).
        - Bytes 2-9: LBA via BigEndian.PutUint64.
        - Byte 13: number of logical blocks (uint8).
        - ExpectedDataTransferLen = 2 * uint32(blocks) * blockSize (compare data + write data).
        - Write=true. Data=data. LUN=lun.

    11. `StartStopUnit(lun uint64, powerCondition uint8, start, loadEject bool, opts ...Option) session.Command`:
        - CDB byte 0 = 0x1B (OpStartStopUnit).
        - Byte 1: IMMED at bit 0 (0x01).
        - Byte 4: power condition in bits 7-4 (powerCondition << 4), LOEJ at bit 1, START at bit 0.
        - No data transfer. LUN=lun.

    **reservations_test.go** - Table-driven tests:
    - TestPersistReserveIn: READ KEYS service action, READ RESERVATION, allocation length placement
    - TestParsePersistReserveInKeys: header + 2 keys, empty keys list
    - TestParsePersistReserveInReservation: active reservation, no reservation (additional length 0)
    - TestPersistReserveOut: REGISTER action, verify 24-byte parameter data (read cmd.Data), verify key and saKey placement, RESERVE/RELEASE actions

    **commands_test.go** - Add tests (append to existing file):
    - TestVerify10: CDB bytes, BYTCHK flag placement (byte 1 bits 2-1)
    - TestVerify16: 64-bit LBA, BYTCHK
    - TestCompareAndWrite: CDB bytes, verify ExpectedDataTransferLen = 2*blocks*blockSize, blocks in byte 13
    - TestStartStopUnit: START=true, LOEJ=true, power condition values, IMMED option
  </action>
  <verify>
    <automated>cd /home/rkujawa/repos/uiscsi && go test ./internal/scsi/ -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "func PersistReserveIn" internal/scsi/reservations.go
    - grep -q "func PersistReserveOut" internal/scsi/reservations.go
    - grep -q "func ParsePersistReserveInKeys" internal/scsi/reservations.go
    - grep -q "PROutRegister" internal/scsi/reservations.go
    - grep -q "0x5E" internal/scsi/reservations.go (PR IN opcode)
    - grep -q "0x5F" internal/scsi/reservations.go (PR OUT opcode)
    - grep -q "func Verify10" internal/scsi/commands.go
    - grep -q "func Verify16" internal/scsi/commands.go
    - grep -q "func CompareAndWrite" internal/scsi/commands.go
    - grep -q "func StartStopUnit" internal/scsi/commands.go
    - grep -q "0x89" internal/scsi/commands.go (CAW opcode)
    - grep -q "0x1B" internal/scsi/commands.go (SSU opcode)
    - go test ./internal/scsi/ -count=1 exits 0
    - go test ./internal/scsi/ -count=1 -race exits 0
  </acceptance_criteria>
  <done>All extended SCSI commands implemented. PR IN/OUT with service actions and 24-byte parameter data (Pitfall 7). COMPARE AND WRITE with 2x transfer length (Pitfall 8). VERIFY with BYTCHK. START STOP UNIT with power conditions. Full test suite passes under -race.</done>
</task>

</tasks>

<verification>
cd /home/rkujawa/repos/uiscsi && go test ./internal/scsi/ -count=1 -race
cd /home/rkujawa/repos/uiscsi && go vet ./internal/scsi/
cd /home/rkujawa/repos/uiscsi && go test ./... -count=1 -race
</verification>

<success_criteria>
- SYNCHRONIZE CACHE 10/16, WRITE SAME 10/16, UNMAP, VERIFY 10/16, PR IN/OUT, COMPARE AND WRITE, START STOP UNIT all produce correct CDBs
- UNMAP parameter data correctly serialized (Pitfall 6)
- PR OUT 24-byte parameter data correctly serialized (Pitfall 7)
- COMPARE AND WRITE transfer length = 2*blocks*blockSize (Pitfall 8)
- All extended command tests pass under -race
- Full project test suite still passes (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/05-scsi-command-layer/05-03-SUMMARY.md`
</output>
