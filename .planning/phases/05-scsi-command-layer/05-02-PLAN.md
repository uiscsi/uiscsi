---
phase: 05-scsi-command-layer
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - internal/scsi/readwrite.go
  - internal/scsi/readwrite_test.go
  - internal/scsi/vpd.go
  - internal/scsi/vpd_test.go
  - internal/scsi/inquiry.go
autonomous: true
requirements:
  - SCSI-03
  - SCSI-05
  - SCSI-06
  - SCSI-18

must_haves:
  truths:
    - "READ(10) produces CDB with opcode 0x28, LBA in bytes 2-5, transfer length in bytes 7-8"
    - "READ(16) produces CDB with opcode 0x88, LBA in bytes 2-9, transfer length in bytes 10-13"
    - "WRITE(10) produces CDB with opcode 0x2A, sets Write=true and Data=reader"
    - "WRITE(16) produces CDB with opcode 0x8A, 64-bit LBA in bytes 2-9"
    - "FUA and DPO options set correct bits in CDB byte 1"
    - "INQUIRY VPD (EVPD=1) produces CDB with byte 1 bit 0 set and page code in byte 2"
    - "VPD page 0x00 parsed into list of supported page codes"
    - "VPD page 0x80 parsed into unit serial number string"
    - "VPD page 0x83 parsed into typed Designator list with variable-length walking"
    - "VPD page 0xB0 parsed into block limits (max transfer, optimal, unmap granularity)"
    - "VPD page 0xB1 parsed into block characteristics (rotation rate, form factor)"
    - "VPD page 0xB2 parsed into logical block provisioning (thin provisioning, UNMAP support)"
  artifacts:
    - path: "internal/scsi/readwrite.go"
      provides: "Read10, Read16, Write10, Write16 CDB builders"
      exports: ["Read10", "Read16", "Write10", "Write16"]
    - path: "internal/scsi/vpd.go"
      provides: "VPD page CDB builder and parsers for pages 0x00, 0x80, 0x83, 0xB0, 0xB1, 0xB2"
      exports: ["InquiryVPD", "ParseVPDSupportedPages", "ParseVPDSerialNumber", "ParseVPDDeviceIdentification", "ParseVPDBlockLimits", "ParseVPDBlockCharacteristics", "ParseVPDLogicalBlockProvisioning"]
  key_links:
    - from: "internal/scsi/readwrite.go"
      to: "internal/scsi/opcode.go"
      via: "Option type for FUA/DPO flags"
      pattern: "WithFUA|WithDPO"
    - from: "internal/scsi/vpd.go"
      to: "internal/scsi/inquiry.go"
      via: "Shares INQUIRY opcode, VPD dispatch from InquiryVPD builder"
      pattern: "OpInquiry"
---

<objective>
Implement READ/WRITE 10/16 CDB builders and all VPD page parsers (standard pages 0x00, 0x80, 0x83 and extended pages 0xB0, 0xB1, 0xB2). These are the data transfer commands and device identification pages that form the core of block storage interaction.

Purpose: Complete the read/write command builders needed for actual I/O and the VPD parsers needed for device identification and capability discovery.
Output: readwrite.go, vpd.go + tests, updated inquiry.go with VPD CDB builder
</objective>

<execution_context>
@/home/rkujawa/repos/uiscsi/.claude/get-shit-done/workflows/execute-plan.md
@/home/rkujawa/repos/uiscsi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-scsi-command-layer/05-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output - executor reads these files -->
From internal/scsi/opcode.go (created in Plan 01):
- OpRead10 = 0x28, OpRead16 = 0x88, OpWrite10 = 0x2A, OpWrite16 = 0x8A, OpInquiry = 0x12
- Option type, WithFUA(), WithDPO() functional options
- checkResult(result) helper for parse functions
- StatusGood, CommandError, session.Command, session.Result

From internal/session/types.go:
- Command.Data io.Reader (write payload)
- Command.Read, Command.Write bool
- Command.ExpectedDataTransferLen uint32
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: READ and WRITE 10/16 CDB builders</name>
  <files>internal/scsi/readwrite.go, internal/scsi/readwrite_test.go</files>
  <read_first>internal/scsi/opcode.go, internal/session/types.go, .planning/phases/05-scsi-command-layer/05-RESEARCH.md</read_first>
  <behavior>
    - Read10(lun=0, lba=0, blocks=1, blockSize=512): CDB=[0x28,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x01,0x00,...], Read=true, ExpectedDataTransferLen=512
    - Read10(lun=1, lba=0x01000000, blocks=256, blockSize=4096, WithFUA()): CDB byte 1 = 0x08, ExpectedDataTransferLen=1048576
    - Read10 with WithDPO(): CDB byte 1 bit 4 set (0x10)
    - Read10 with WithFUA()+WithDPO(): CDB byte 1 = 0x18
    - Read16(lun=0, lba=0x0000000100000000, blocks=1, blockSize=512): CDB=[0x88,0x00, lba 8 bytes, 0x00,0x00,0x00,0x01,...], Read=true
    - Write10(lun=0, lba=100, blocks=8, blockSize=512, data): CDB=[0x2A,...], Write=true, Data=data, ExpectedDataTransferLen=4096
    - Write16 with FUA: CDB byte 1 = 0x08, Write=true
    - LUN field correctly set on all commands
  </behavior>
  <action>
    Per D-03 (plain functions, positional args) and D-04 (functional options for flags):

    **readwrite.go**:

    1. `Read10(lun uint64, lba uint32, blocks uint16, blockSize uint32, opts ...Option) session.Command`:
       - CDB byte 0 = 0x28 (OpRead10)
       - Byte 1: DPO at bit 4 (0x10), FUA at bit 3 (0x08)
       - Bytes 2-5: LBA via BigEndian.PutUint32
       - Byte 6: group number = 0
       - Bytes 7-8: transfer length via BigEndian.PutUint16
       - Read=true, ExpectedDataTransferLen = uint32(blocks) * blockSize, LUN=lun

    2. `Read16(lun uint64, lba uint64, blocks uint32, blockSize uint32, opts ...Option) session.Command`:
       - CDB byte 0 = 0x88 (OpRead16)
       - Byte 1: DPO at bit 4, FUA at bit 3
       - Bytes 2-9: LBA via BigEndian.PutUint64
       - Bytes 10-13: transfer length via BigEndian.PutUint32
       - Read=true, ExpectedDataTransferLen = blocks * blockSize, LUN=lun

    3. `Write10(lun uint64, lba uint32, blocks uint16, blockSize uint32, data io.Reader, opts ...Option) session.Command`:
       - CDB byte 0 = 0x2A (OpWrite10)
       - Byte 1: DPO at bit 4, FUA at bit 3 (same layout as READ)
       - Bytes 2-5: LBA. Bytes 7-8: transfer length.
       - Write=true, Data=data, ExpectedDataTransferLen = uint32(blocks) * blockSize, LUN=lun

    4. `Write16(lun uint64, lba uint64, blocks uint32, blockSize uint32, data io.Reader, opts ...Option) session.Command`:
       - CDB byte 0 = 0x8A (OpWrite16)
       - Same layout as Read16 but with Write direction
       - Bytes 2-9: LBA. Bytes 10-13: transfer length.
       - Write=true, Data=data, LUN=lun

    **readwrite_test.go** - Golden byte table-driven tests:
    - TestRead10: LBA=0/blocks=1/512, high LBA with FUA, DPO+FUA combined, LUN setting
    - TestRead16: 64-bit LBA beyond 32-bit range, FUA flag
    - TestWrite10: verify Write=true, Data set, CDB bytes, FUA flag
    - TestWrite16: 64-bit LBA, ExpectedDataTransferLen calculation
    - All tests verify exact CDB bytes, direction flags, transfer length, LUN
  </action>
  <verify>
    <automated>cd /home/rkujawa/repos/uiscsi && go test ./internal/scsi/ -run "TestRead|TestWrite" -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "func Read10" internal/scsi/readwrite.go
    - grep -q "func Read16" internal/scsi/readwrite.go
    - grep -q "func Write10" internal/scsi/readwrite.go
    - grep -q "func Write16" internal/scsi/readwrite.go
    - grep -q "0x28" internal/scsi/readwrite.go (OpRead10)
    - grep -q "0x88" internal/scsi/readwrite.go (OpRead16)
    - grep -q "0x2A" internal/scsi/readwrite.go (OpWrite10)
    - grep -q "0x8A" internal/scsi/readwrite.go (OpWrite16)
    - grep -q "io.Reader" internal/scsi/readwrite.go (write data parameter)
    - go test ./internal/scsi/ -run "TestRead10" -count=1 exits 0
    - go test ./internal/scsi/ -run "TestWrite10" -count=1 exits 0
  </acceptance_criteria>
  <done>READ/WRITE 10/16 CDB builders produce correct byte layouts with FUA/DPO options. Write commands set Write=true and accept io.Reader data. All golden byte tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: VPD page CDB builder and response parsers (standard + extended)</name>
  <files>internal/scsi/vpd.go, internal/scsi/vpd_test.go, internal/scsi/inquiry.go</files>
  <read_first>internal/scsi/opcode.go, internal/scsi/inquiry.go, internal/session/types.go, .planning/phases/05-scsi-command-layer/05-RESEARCH.md</read_first>
  <behavior>
    - InquiryVPD(lun, pageCode=0x00, allocLen=252): CDB=[0x12, 0x01, 0x00, 0x00, 0xFC, 0x00,...], Read=true, EVPD bit set
    - InquiryVPD(lun, pageCode=0x83, allocLen=252): CDB byte 2 = 0x83
    - ParseVPDSupportedPages: data=[0x00, 0x00, 0x00, 0x03, 0x00, 0x80, 0x83] -> []uint8{0x00, 0x80, 0x83}
    - ParseVPDSerialNumber: data=[0x00, 0x80, 0x00, 0x08, "A","B","C","1","2","3","4","5"] -> "ABC12345"
    - ParseVPDDeviceIdentification: multi-descriptor response -> []Designator with CodeSet, ProtocolID, Type, Association, Identifier
    - ParseVPDDeviceIdentification: correctly walks variable-length descriptors using byte 3 length field (Pitfall 5)
    - ParseVPDBlockLimits(0xB0): max transfer length, optimal transfer length/granularity, max unmap LBA count, max unmap block descriptor count, optimal unmap granularity
    - ParseVPDBlockCharacteristics(0xB1): medium rotation rate, nominal form factor
    - ParseVPDLogicalBlockProvisioning(0xB2): thin provisioning type, UNMAP support bit, WRITE SAME support bits
  </behavior>
  <action>
    Per D-03, D-05 (parse common fields, raw for niche), D-08 (parse takes session.Result):

    **inquiry.go** - Add VPD CDB builder (update existing file from Plan 01):

    1. `InquiryVPD(lun uint64, pageCode uint8, allocLen uint16) session.Command`:
       - CDB byte 0 = 0x12 (OpInquiry). Byte 1 = 0x01 (EVPD bit 0 set). Byte 2 = pageCode.
       - Bytes 3-4 = allocLen (BigEndian). Read=true. LUN = lun.

    **vpd.go** - VPD page parsers:

    1. `ParseVPDSupportedPages(result session.Result) ([]uint8, error)`:
       - checkResult, require >= 4 bytes. Page length at byte 3. Return data[4:4+pageLen] as []uint8.

    2. `ParseVPDSerialNumber(result session.Result) (string, error)`:
       - checkResult, require >= 4 bytes. Page length at byte 3. Return string(data[4:4+pageLen]) trimmed.

    3. Designator struct:
       ```go
       type Designator struct {
           CodeSet      uint8  // bits 3-0 of byte 0
           ProtocolID   uint8  // bits 7-4 of byte 0
           Type         uint8  // bits 3-0 of byte 1
           Association  uint8  // bits 5-4 of byte 1
           Identifier   []byte
       }
       ```

    4. `ParseVPDDeviceIdentification(result session.Result) ([]Designator, error)`:
       - checkResult, require >= 4 bytes. Page length at bytes 2-3 (BigEndian.Uint16 for 0x83).
       - Walk descriptor list per Pitfall 5: each descriptor has 4-byte header. Byte 3 = identifier length.
         Next descriptor at offset + 4 + identifierLength. Collect into []Designator.
       - Guard against malformed descriptors (length exceeding remaining data).

    5. `BlockLimits` struct (VPD 0xB0):
       ```go
       type BlockLimits struct {
           MaxCompareAndWriteLength uint8   // byte 5
           OptimalTransferLengthGranularity uint16 // bytes 6-7
           MaxTransferLength        uint32  // bytes 8-11
           OptimalTransferLength    uint32  // bytes 12-15
           MaxUnmapLBACount         uint32  // bytes 20-23
           MaxUnmapBlockDescCount   uint32  // bytes 24-27
           OptimalUnmapGranularity  uint32  // bytes 28-31
           Raw                      []byte
       }
       ```

    6. `ParseVPDBlockLimits(result session.Result) (*BlockLimits, error)`:
       - checkResult, VPD header 4 bytes + at least 60 bytes (0xB0 minimum). Parse fields at documented offsets.
       - MaxTransferLength at bytes 8-11, OptimalTransferLength at bytes 12-15 (relative to data start after header? No -- offsets are from page start including header). Actually: VPD response includes 4-byte VPD header, then page-specific data. For 0xB0, the SBC-3 offsets are relative to byte 0 of the page data. So MaxTransferLength is at response bytes 8-11 (4 header + 4 offset). Be precise: parse by absolute offset in the response data.

    7. `BlockCharacteristics` struct (VPD 0xB1):
       ```go
       type BlockCharacteristics struct {
           MediumRotationRate uint16  // bytes 4-5 (0x0000=not reported, 0x0001=non-rotating/SSD)
           NominalFormFactor  uint8   // byte 7 bits 3-0
           Raw                []byte
       }
       ```

    8. `ParseVPDBlockCharacteristics(result session.Result) (*BlockCharacteristics, error)`:
       - checkResult, require >= 8 bytes. MediumRotationRate at bytes 4-5, NominalFormFactor at byte 7 & 0x0F.

    9. `LogicalBlockProvisioning` struct (VPD 0xB2):
       ```go
       type LogicalBlockProvisioning struct {
           ThresholdExponent uint8  // byte 4
           LBPU              bool   // byte 5 bit 7 (UNMAP command supported)
           LBPWS             bool   // byte 5 bit 6 (WRITE SAME with UNMAP supported)
           LBPWS10           bool   // byte 5 bit 5 (WRITE SAME(10) with UNMAP)
           ProvisioningType  uint8  // byte 6 bits 2-0
           Raw               []byte
       }
       ```

    10. `ParseVPDLogicalBlockProvisioning(result session.Result) (*LogicalBlockProvisioning, error)`:
        - checkResult, require >= 8 bytes. Parse bits from documented offsets.

    **vpd_test.go** - Table-driven tests:
    - TestInquiryVPD: CDB bytes with EVPD set, various page codes
    - TestParseVPDSupportedPages: normal response, empty list
    - TestParseVPDSerialNumber: ASCII serial, trailing spaces trimmed
    - TestParseVPDDeviceIdentification: single descriptor, multiple descriptors (walk variable lengths per Pitfall 5), malformed (length exceeds data)
    - TestParseVPDBlockLimits: full 0xB0 page with known field values
    - TestParseVPDBlockCharacteristics: SSD (rotation=1), HDD (rotation=7200)
    - TestParseVPDLogicalBlockProvisioning: thin provisioned with UNMAP support, fully provisioned
  </action>
  <verify>
    <automated>cd /home/rkujawa/repos/uiscsi && go test ./internal/scsi/ -run "TestInquiryVPD|TestVPD|TestRead|TestWrite" -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "func InquiryVPD" internal/scsi/inquiry.go
    - grep -q "0x01" internal/scsi/inquiry.go (EVPD bit)
    - grep -q "func ParseVPDSupportedPages" internal/scsi/vpd.go
    - grep -q "func ParseVPDSerialNumber" internal/scsi/vpd.go
    - grep -q "func ParseVPDDeviceIdentification" internal/scsi/vpd.go
    - grep -q "type Designator struct" internal/scsi/vpd.go
    - grep -q "func ParseVPDBlockLimits" internal/scsi/vpd.go
    - grep -q "func ParseVPDBlockCharacteristics" internal/scsi/vpd.go
    - grep -q "func ParseVPDLogicalBlockProvisioning" internal/scsi/vpd.go
    - grep -q "MediumRotationRate" internal/scsi/vpd.go
    - grep -q "LBPU" internal/scsi/vpd.go (UNMAP support bit)
    - go test ./internal/scsi/ -run "TestVPD" -count=1 exits 0
    - go test ./internal/scsi/ -count=1 exits 0
  </acceptance_criteria>
  <done>READ/WRITE 10/16 builders produce correct CDBs with FUA/DPO. VPD CDB builder sets EVPD bit. All 6 VPD pages (0x00, 0x80, 0x83, 0xB0, 0xB1, 0xB2) have typed parsers. Variable-length descriptor walking handles Pitfall 5 correctly. All tests pass.</done>
</task>

</tasks>

<verification>
cd /home/rkujawa/repos/uiscsi && go test ./internal/scsi/ -count=1 -race
cd /home/rkujawa/repos/uiscsi && go vet ./internal/scsi/
</verification>

<success_criteria>
- Read10, Read16, Write10, Write16 produce correct CDB bytes with FUA/DPO options
- Write commands set Write=true and Data=io.Reader
- InquiryVPD sets EVPD bit and page code
- All 6 VPD pages parsed into typed structs
- VPD 0x83 descriptor walking handles variable lengths
- Extended VPD (0xB0, 0xB1, 0xB2) parse thin provisioning and block limits
- All tests pass under -race
</success_criteria>

<output>
After completion, create `.planning/phases/05-scsi-command-layer/05-02-SUMMARY.md`
</output>
