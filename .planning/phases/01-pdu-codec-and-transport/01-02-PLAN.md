---
phase: 01-pdu-codec-and-transport
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - internal/pdu/opcode.go
  - internal/pdu/opcode_test.go
  - internal/pdu/bhs.go
  - internal/pdu/bhs_test.go
  - internal/pdu/header.go
  - internal/pdu/initiator.go
  - internal/pdu/target.go
  - internal/pdu/ahs.go
  - internal/pdu/ahs_test.go
  - internal/pdu/pdu.go
  - internal/pdu/pdu_test.go
autonomous: true
requirements: [PDU-01, PDU-04, TEST-03]

must_haves:
  truths:
    - "All 24 iSCSI opcodes can be round-trip encoded and decoded with byte-perfect BHS fidelity"
    - "PDU data segments are padded to 4-byte boundaries with zero bytes"
    - "AHS (Additional Header Segments) are correctly encoded and decoded"
    - "DataSegmentLength is encoded as 24-bit big-endian in bytes 5-7 without corrupting TotalAHSLength in byte 4"
    - "Table-driven unit tests cover all 24 PDU types including edge cases"
  artifacts:
    - path: "internal/pdu/opcode.go"
      provides: "Opcode constants for all 24 iSCSI opcodes"
      contains: "OpSCSICommand"
    - path: "internal/pdu/header.go"
      provides: "PDU interface and Header base struct"
      exports: ["PDU", "Header"]
    - path: "internal/pdu/initiator.go"
      provides: "All 8 initiator opcode struct types with MarshalBHS/UnmarshalBHS"
      min_lines: 200
    - path: "internal/pdu/target.go"
      provides: "All 10 target opcode struct types with MarshalBHS/UnmarshalBHS"
      min_lines: 200
    - path: "internal/pdu/pdu.go"
      provides: "Top-level encode/decode dispatching by opcode"
      exports: ["EncodePDU", "DecodeBHS"]
    - path: "internal/pdu/pdu_test.go"
      provides: "Round-trip tests for all 24 PDU types"
      min_lines: 300
  key_links:
    - from: "internal/pdu/pdu.go"
      to: "internal/pdu/initiator.go"
      via: "opcode dispatch in DecodeBHS"
      pattern: "OpSCSICommand|OpNOPOut"
    - from: "internal/pdu/pdu.go"
      to: "internal/pdu/target.go"
      via: "opcode dispatch in DecodeBHS"
      pattern: "OpSCSIResponse|OpNOPIn"
    - from: "internal/pdu/bhs.go"
      to: "encoding/binary"
      via: "BigEndian for 16/32/64-bit BHS fields"
      pattern: "binary\\.BigEndian"
---

<objective>
Implement the complete PDU codec: typed Go structs for all 24 iSCSI opcodes (8 initiator, 10 target including Reject) with BHS marshal/unmarshal, AHS support, a common PDU interface, and top-level encode/decode dispatch. Per D-03, all opcodes are implemented from day one.

Purpose: The PDU codec is the core data model consumed by every subsequent phase -- transport framing, login negotiation, session management, read/write paths, and error recovery all operate on these types.
Output: Complete internal/pdu/ package with all types, encode/decode, and comprehensive round-trip tests.
</objective>

<execution_context>
@/home/rkujawa/repos/uiscsi/.claude/get-shit-done/workflows/execute-plan.md
@/home/rkujawa/repos/uiscsi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-pdu-codec-and-transport/01-CONTEXT.md
@.planning/phases/01-pdu-codec-and-transport/01-RESEARCH.md

Depends on Plan 01 output:
@.planning/phases/01-pdu-codec-and-transport/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 -- padding helper this plan uses -->
From internal/pdu/padding.go:
```go
func PadLen(n uint32) uint32  // returns (4 - (n % 4)) % 4
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Opcode constants, BHS helpers, PDU interface, Header base struct, and AHS -- with unit tests</name>
  <files>internal/pdu/opcode.go, internal/pdu/opcode_test.go, internal/pdu/bhs.go, internal/pdu/bhs_test.go, internal/pdu/header.go, internal/pdu/ahs.go, internal/pdu/ahs_test.go</files>
  <read_first>
    internal/pdu/padding.go
    .planning/phases/01-pdu-codec-and-transport/01-RESEARCH.md
  </read_first>
  <behavior>
    - OpSCSICommand.String() == "SCSI Command"
    - OpNOPOut.IsInitiator() == true, OpNOPOut.IsTarget() == false
    - OpNOPIn.IsTarget() == true, OpNOPIn.IsInitiator() == false
    - OpReject.IsTarget() == true
    - OpSNACKReq.IsInitiator() == true
    - All 18 opcode constants have distinct values (no collisions)
    - encodeDataSegmentLength(bhs, 0x123456) sets bhs[5]=0x12, bhs[6]=0x34, bhs[7]=0x56 and does NOT touch bhs[4]
    - decodeDataSegmentLength round-trips with encodeDataSegmentLength for values 0, 1, 0xFFFFFF, 0x000100
    - encodeDataSegmentLength with bhs[4]=0xAA (TotalAHSLength) preserves bhs[4] after encoding (Pitfall 2 regression)
    - encodeOpcodeByte(OpSCSICommand, false) == 0x01
    - encodeOpcodeByte(OpSCSICommand, true) == 0x41 (immediate bit set)
    - decodeOpcodeByte(0x41) == (OpSCSICommand, true)
    - decodeOpcodeByte(0x01) == (OpSCSICommand, false)
    - MarshalAHS(nil) returns empty/nil byte slice
    - MarshalAHS of one ExtendedCDB AHS with 20 bytes data produces 4-byte-aligned output
    - UnmarshalAHS round-trips with MarshalAHS for single and multiple AHS segments
    - UnmarshalAHS on empty input returns nil/empty slice and no error
    - UnmarshalAHS on truncated input returns error
  </behavior>
  <action>
    1. Create `internal/pdu/opcode.go` defining all 24 opcode constants as a typed `OpCode` type (uint8):

       ```go
       type OpCode uint8

       const (
           // Initiator opcodes (8)
           OpNOPOut          OpCode = 0x00
           OpSCSICommand     OpCode = 0x01
           OpTaskMgmtReq     OpCode = 0x02
           OpLoginReq        OpCode = 0x03
           OpTextReq         OpCode = 0x04
           OpDataOut         OpCode = 0x05
           OpLogoutReq       OpCode = 0x06
           OpSNACKReq        OpCode = 0x10

           // Target opcodes (10)
           OpNOPIn           OpCode = 0x20
           OpSCSIResponse    OpCode = 0x21
           OpTaskMgmtResp    OpCode = 0x22
           OpLoginResp       OpCode = 0x23
           OpTextResp        OpCode = 0x24
           OpDataIn          OpCode = 0x25
           OpLogoutResp      OpCode = 0x26
           OpR2T             OpCode = 0x31
           OpAsyncMsg        OpCode = 0x32
           OpReject          OpCode = 0x3f
       )
       ```
       Add a `String() string` method on OpCode returning human-readable names (e.g., "NOP-Out", "SCSI Command", etc.).
       Add `IsInitiator() bool` -- returns true for opcodes 0x00-0x3f where bit 5 is 0.
       Add `IsTarget() bool` -- returns true for opcodes where bit 5 is 1.

    2. Create `internal/pdu/opcode_test.go` with table-driven tests:
       - Verify String() output for all 18 opcodes (non-empty, human-readable)
       - Verify IsInitiator()/IsTarget() classification for all 18 opcodes
       - Verify all opcode values are distinct (collect into a map, check for duplicates)

    3. Create `internal/pdu/bhs.go` with BHS constants and encoding helpers:
       - `const BHSLength = 48`
       - Helper functions for the 24-bit DataSegmentLength field (bytes 5-7):
         ```go
         func encodeDataSegmentLength(bhs []byte, dsLen uint32) {
             bhs[5] = byte(dsLen >> 16)
             bhs[6] = byte(dsLen >> 8)
             bhs[7] = byte(dsLen)
         }
         func decodeDataSegmentLength(bhs []byte) uint32 {
             return uint32(bhs[5])<<16 | uint32(bhs[6])<<8 | uint32(bhs[7])
         }
         ```
       CRITICAL (Pitfall 2): DataSegmentLength is bytes 5-7 only (24-bit). Do NOT use binary.BigEndian.PutUint32 which would overwrite byte 4 (TotalAHSLength).

       - Helper for encoding/decoding the first byte (Immediate bit + opcode):
         ```go
         func encodeOpcodeByte(opcode OpCode, immediate bool) byte {
             b := byte(opcode) & 0x3f
             if immediate { b |= 0x40 }
             return b
         }
         func decodeOpcodeByte(b byte) (OpCode, bool) {
             return OpCode(b & 0x3f), b&0x40 != 0
         }
         ```

    4. Create `internal/pdu/bhs_test.go` with table-driven tests:
       - encodeDataSegmentLength/decodeDataSegmentLength round-trip for values: 0, 1, 255, 256, 0x000100, 0x123456, 0xFFFFFF
       - REGRESSION (Pitfall 2): Set bhs[4]=0xAA before encodeDataSegmentLength, verify bhs[4] is still 0xAA after encode -- proves DataSegmentLength encoding does not corrupt TotalAHSLength
       - encodeOpcodeByte/decodeOpcodeByte round-trip for all 18 opcodes, with and without immediate bit
       - Verify encodeOpcodeByte masks to 6 bits (upper 2 bits are immediate + reserved)

    5. Create `internal/pdu/header.go` defining the common PDU interface and Header base:
       ```go
       // PDU is the interface implemented by all iSCSI PDU types.
       type PDU interface {
           Opcode() OpCode
           MarshalBHS() ([BHSLength]byte, error)
           DataSegment() []byte
       }

       // Header contains BHS fields common to all PDU types.
       type Header struct {
           Immediate      bool
           OpCode_        OpCode  // underscore to avoid collision with Opcode() method
           Final          bool
           TotalAHSLength uint8   // in 4-byte words
           DataSegmentLen uint32  // 24-bit max (0x00FFFFFF)
           LUN            [8]byte
           InitiatorTaskTag uint32
       }
       ```
       Add a `DecodeBHS` function that takes a `[48]byte` and returns a concrete PDU type based on the opcode byte. This function:
       - Reads byte 0 to get opcode and immediate flag
       - Switches on opcode to create the correct typed struct
       - Calls that type's UnmarshalBHS method
       - Returns the PDU interface

    6. Create `internal/pdu/ahs.go` with AHS (Additional Header Segment) types:
       ```go
       // AHSType identifies the type of additional header segment.
       type AHSType uint8
       const (
           AHSExtendedCDB     AHSType = 1
           AHSBidiReadDataLen AHSType = 2
       )

       // AHS represents a single Additional Header Segment.
       type AHS struct {
           Type    AHSType
           Length  uint16  // total AHS length in bytes (including type+length fields)
           Data    []byte
       }
       ```
       Add `MarshalAHS(segments []AHS) []byte` and `UnmarshalAHS(data []byte) ([]AHS, error)` functions. AHS segments are 4-byte aligned. Most PDUs in Phase 1 will have no AHS -- but the codec must handle them for correctness.

    7. Create `internal/pdu/ahs_test.go` with table-driven tests:
       - MarshalAHS(nil) returns empty/nil
       - Single AHS segment round-trip (ExtendedCDB with 20 bytes data)
       - Multiple AHS segments round-trip
       - Verify MarshalAHS output is 4-byte aligned
       - UnmarshalAHS on empty input returns nil and no error
       - UnmarshalAHS on truncated/corrupt input returns error

    TDD flow: Write test files first (RED), then implement production code (GREEN).
  </action>
  <verify>
    <automated>cd /home/rkujawa/repos/uiscsi && go test -race -count=1 ./internal/pdu/ -run "TestOpcode|TestBHS|TestAHS|TestEncode|TestDecode" -v && go vet ./internal/pdu/</automated>
  </verify>
  <acceptance_criteria>
    - internal/pdu/opcode.go contains all 18 unique opcode constants (OpNOPOut through OpReject)
    - internal/pdu/opcode.go contains `type OpCode uint8`
    - internal/pdu/opcode.go contains `func (o OpCode) String() string`
    - internal/pdu/opcode_test.go contains tests for String(), IsInitiator(), IsTarget()
    - internal/pdu/bhs.go contains `const BHSLength = 48`
    - internal/pdu/bhs.go contains `encodeDataSegmentLength` with 3-byte encoding (bytes 5-7)
    - internal/pdu/bhs.go does NOT contain `PutUint32` applied to bytes 4-7 for DataSegmentLength
    - internal/pdu/bhs_test.go contains `0xAA` (TotalAHSLength corruption regression test)
    - internal/pdu/bhs_test.go tests encodeDataSegmentLength/decodeDataSegmentLength round-trip
    - internal/pdu/bhs_test.go tests encodeOpcodeByte/decodeOpcodeByte round-trip
    - internal/pdu/header.go contains `type PDU interface`
    - internal/pdu/header.go contains `MarshalBHS() ([BHSLength]byte, error)` in the interface
    - internal/pdu/header.go contains `DataSegment() []byte` in the interface
    - internal/pdu/header.go contains `func DecodeBHS(`
    - internal/pdu/ahs.go contains `type AHS struct`
    - internal/pdu/ahs_test.go contains round-trip AHS tests
    - `go test -race ./internal/pdu/ -run "TestOpcode|TestBHS|TestAHS"` exits 0
    - `go vet ./internal/pdu/` exits 0
  </acceptance_criteria>
  <done>All opcode constants defined and tested, BHS encode/decode helpers verified with round-trip tests including TotalAHSLength corruption regression, PDU interface and Header base defined, DecodeBHS dispatches by opcode, AHS marshal/unmarshal tested with round-trips, go test -race and go vet pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: All 24 opcode types with MarshalBHS/UnmarshalBHS and round-trip tests</name>
  <files>internal/pdu/initiator.go, internal/pdu/target.go, internal/pdu/pdu.go, internal/pdu/pdu_test.go</files>
  <read_first>
    internal/pdu/opcode.go
    internal/pdu/bhs.go
    internal/pdu/header.go
    internal/pdu/ahs.go
    internal/pdu/padding.go
    .planning/phases/01-pdu-codec-and-transport/01-RESEARCH.md
  </read_first>
  <behavior>
    - For each of 24 PDU types: MarshalBHS then UnmarshalBHS produces identical field values (round-trip)
    - NOP-Out with ITT=0xDEADBEEF and TTT=0xCAFEBABE round-trips correctly
    - SCSI Command with CDB filled, LUN set, ExpectedDataLen=65536 round-trips correctly
    - Login Request with ISID, TSIH, CSG=1, NSG=3, T=true round-trips correctly
    - Data-In with S-bit, F-bit, DataSN, BufferOffset round-trips correctly
    - Reject with reason code and 48-byte rejected header in data segment round-trips correctly
    - PDU with zero-length data segment encodes with DataSegmentLen=0 and no padding
    - PDU with data segment length 5 produces 3 bytes of zero padding
    - Immediate bit set on applicable PDUs encodes in byte 0 bit 6
    - Final bit set encodes in byte 1 bit 7
  </behavior>
  <action>
    1. Create `internal/pdu/initiator.go` with typed structs for all 8 initiator opcodes. Each struct embeds Header and adds opcode-specific fields. Each implements the PDU interface (Opcode(), MarshalBHS(), DataSegment()). Each has an UnmarshalBHS method taking [48]byte.

       The 8 initiator types and their opcode-specific BHS fields (bytes 20-47):
       - **NOPOut** (0x00): TTT (bytes 20-23), CmdSN (24-27), ExpStatSN (28-31). Ping data in data segment. ITT=0xFFFFFFFF for unsolicited.
       - **SCSICommand** (0x01): Flags byte 1 (R bit 6, W bit 5, Attr bits 2-0), ExpectedDataTransferLength (20-23), CmdSN (24-27), ExpStatSN (28-31), CDB[16] (32-47). Immediate data in data segment.
       - **TaskMgmtReq** (0x02): Function (byte 1 bits 6-0), ReferencedTaskTag (20-23), CmdSN (24-27), ExpStatSN (28-31), RefCmdSN (32-35). LUN in bytes 8-15.
       - **LoginReq** (0x03): Flags byte 1 (T bit 7, C bit 6, CSG bits 3-2, NSG bits 1-0), VersionMax (byte 2), VersionMin (byte 3), ISID (bytes 8-13), TSIH (bytes 14-15), CID (bytes 20-21), CmdSN (24-27), ExpStatSN (28-31). Key-value pairs in data segment.
       - **TextReq** (0x04): Flags byte 1 (F bit 7, C bit 6), TTT (20-23), CmdSN (24-27), ExpStatSN (28-31). Key-value pairs in data segment.
       - **DataOut** (0x05): Flags byte 1 (F bit 7), TTT (20-23), ExpStatSN (28-31), DataSN (36-39), BufferOffset (40-43). Write data in data segment.
       - **LogoutReq** (0x06): ReasonCode (byte 1 bits 6-0), CID (bytes 20-21), CmdSN (24-27), ExpStatSN (28-31).
       - **SNACKReq** (0x10): Type (byte 1 bits 3-0), TTT (20-23), ExpStatSN (28-31), BegRun (40-43), RunLength (44-47).

       Use `encoding/binary.BigEndian` for 16/32/64-bit fields. Use manual byte manipulation for sub-byte fields (flags, CSG/NSG bits).

    2. Create `internal/pdu/target.go` with typed structs for all 10 target opcodes:
       - **NOPIn** (0x20): TTT (20-23), StatSN (24-27), ExpCmdSN (28-31), MaxCmdSN (32-35).
       - **SCSIResponse** (0x21): Flags byte 1 (o/u/O/U bits for residuals), Response (byte 2), Status (byte 3), SNACKTag (20-23), StatSN (24-27), ExpCmdSN (28-31), MaxCmdSN (32-35), ExpDataSN (36-39), BidiResidualCount (40-43), ResidualCount (44-47). Sense data in data segment.
       - **TaskMgmtResp** (0x22): Response (byte 2), StatSN (24-27), ExpCmdSN (28-31), MaxCmdSN (32-35).
       - **LoginResp** (0x23): Flags byte 1 (T, C, CSG, NSG same as LoginReq), VersionMax (byte 2), VersionActive (byte 3), ISID (8-13), TSIH (14-15), StatSN (24-27), ExpCmdSN (28-31), MaxCmdSN (32-35), StatusClass (36), StatusDetail (37).
       - **TextResp** (0x24): Flags byte 1 (F, C), TTT (20-23), StatSN (24-27), ExpCmdSN (28-31), MaxCmdSN (32-35).
       - **DataIn** (0x25): Flags byte 1 (F bit 7, A bit 6, S bit 0 -- has status, O bit 2, U bit 3), Status (byte 3 -- only valid if S=1), TTT (20-23), StatSN (24-27 -- only valid if S=1), ExpCmdSN (28-31), MaxCmdSN (32-35), DataSN (36-39), BufferOffset (40-43), ResidualCount (44-47).
       - **LogoutResp** (0x26): Response (byte 2), StatSN (24-27), ExpCmdSN (28-31), MaxCmdSN (32-35), Time2Wait (40-41), Time2Retain (42-43).
       - **R2T** (0x31): TTT (20-23), StatSN (24-27), ExpCmdSN (28-31), MaxCmdSN (32-35), R2TSN (36-39), BufferOffset (40-43), DesiredDataTransferLength (44-47).
       - **AsyncMsg** (0x32): StatSN (24-27), ExpCmdSN (28-31), MaxCmdSN (32-35), AsyncEvent (36), AsyncVCode (37), Parameter1 (38-39), Parameter2 (40-41), Parameter3 (42-43).
       - **Reject** (0x3f): Reason (byte 2), StatSN (24-27), ExpCmdSN (28-31), MaxCmdSN (32-35), DataSN/R2TSN (36-39). Data segment contains the complete BHS of the rejected PDU.

    3. Create `internal/pdu/pdu.go` with top-level convenience functions:
       - `func EncodePDU(p PDU) ([]byte, error)` -- marshals BHS + data segment + padding. Returns complete wire-ready bytes (without digests -- digest is transport layer concern). Computes padding using PadLen.
       - Ensure DecodeBHS in header.go handles all 18 opcode values (some opcodes like 0x07-0x0F are undefined -- return error for unknown opcodes).

    4. Create `internal/pdu/pdu_test.go` with comprehensive table-driven round-trip tests:
       - One test case per opcode type (24 total) with representative field values
       - Edge cases: zero-length data segment, maximum DataSegmentLength (0xFFFFFF), Immediate bit, Final bit
       - Specific regression tests:
         - DataSegmentLength encoding does not corrupt TotalAHSLength (Pitfall 2)
         - Padding for data segments of length 1, 2, 3, 4, 5
         - ITT=0xFFFFFFFF (reserved value) round-trips correctly
         - LoginReq CSG/NSG bit packing in byte 1
         - SCSIResponse residual flags and counts
         - DataIn S-bit with Status field
       - Each test: create struct with known values -> MarshalBHS -> DecodeBHS -> compare all fields

    TDD: Write round-trip test table first (RED), then implement types to pass (GREEN).
  </action>
  <verify>
    <automated>cd /home/rkujawa/repos/uiscsi && go test -race -count=1 ./internal/pdu/ -v</automated>
  </verify>
  <acceptance_criteria>
    - internal/pdu/initiator.go contains structs: NOPOut, SCSICommand, TaskMgmtReq, LoginReq, TextReq, DataOut, LogoutReq, SNACKReq
    - internal/pdu/target.go contains structs: NOPIn, SCSIResponse, TaskMgmtResp, LoginResp, TextResp, DataIn, LogoutResp, R2T, AsyncMsg, Reject
    - Each struct has `MarshalBHS() ([48]byte, error)` method
    - Each struct has `UnmarshalBHS([48]byte)` or equivalent decode method
    - Each struct implements `Opcode() OpCode` returning correct constant
    - internal/pdu/pdu.go contains `func EncodePDU(p PDU) ([]byte, error)`
    - internal/pdu/pdu_test.go contains at least 24 test cases (one per opcode)
    - internal/pdu/pdu_test.go contains `0xFFFFFFFF` (reserved ITT test)
    - internal/pdu/pdu_test.go tests DataSegmentLength does not corrupt TotalAHSLength
    - `go test -race ./internal/pdu/` exits 0
    - `go test -race ./internal/... ` exits 0 (full internal suite)
  </acceptance_criteria>
  <done>All 24 opcode types implemented with correct BHS field layout per RFC 7143 Section 11, round-trip encode/decode tests pass for every type, padding and edge cases verified, go test -race exits 0</done>
</task>

</tasks>

<verification>
- `go test -race -count=1 ./internal/pdu/ -v` -- all 24 PDU types round-trip correctly
- `go test -race -count=1 ./internal/... -v` -- full internal suite (serial + digest + pdu)
- `go vet ./internal/...` -- no vet issues
- `golangci-lint run ./...` -- lint compliance (errcheck, govet, staticcheck, unused, sloglint)
</verification>

<success_criteria>
- All 24 iSCSI opcodes (8 initiator, 10 target) have typed Go structs per D-03
- BHS marshal/unmarshal is byte-perfect for every opcode per D-02
- 24-bit DataSegmentLength encoding does not corrupt adjacent fields
- Padding uses PadLen helper consistently
- Table-driven tests cover every opcode type plus edge cases per TEST-03
- All tests pass under -race
</success_criteria>

<output>
After completion, create `.planning/phases/01-pdu-codec-and-transport/01-02-SUMMARY.md`
</output>
